<%= form_for([@course,@announcement], html: { class: 'form-horizontal' }) do |f| %>
<fieldset>
  <div class="form-group">              
              <span class="col-md-1 col-md-offset-3 text-center"><i class="fa fa-edit bigicon"></i></span>              
              <div class="col-md-5">
                <%= f.text_area :texto, autofocus: true, id: "texto", placeholder: "¿Que estas pensando?", class: "form-control", rows: 4 %>
              </div>
        </div>
  
 <div class="form-group">
   <div class="col-md-8 col-md-offset-4" >
   <%= f.submit "Publicar", class:"btn btn-lg btn-success"  %>
   </div> 
  </div>  
</fieldset>

<% end %>
